"""
斗地主AI项目配置文件

所有可调参数集中在这里，修改后重新运行训练即可生效
"""

# ==========================================
# 游戏配置
# ==========================================
# 卡牌定义（不区分花色，共17种）
# 3-9, O(10), J, Q, K, A, 2, X(小王), D(大王)
ALL_CARDS = ['3', '4', '5', '6', '7', '8', '9', 'O', 'J', 'Q', 'K', 'A', '2', 'X', 'D']
NUM_CARD_TYPES = 17

# 卡牌点数映射（用于比较大小）
CARD_RANK = {
    '3': 1, '4': 2, '5': 3, '6': 4, '7': 5, '8': 6, '9': 7,
    'O': 8,  # 10
    'J': 9, 'Q': 10, 'K': 11, 'A': 12, '2': 13,
    'X': 14,  # 小王
    'D': 15   # 大王
}

# 卡牌数量（一副牌）
CARD_COUNTS = {
    '3': 4, '4': 4, '5': 4, '6': 4, '7': 4, '8': 4, '9': 4,
    'O': 4, 'J': 4, 'Q': 4, 'K': 4, 'A': 4, '2': 4,
    'X': 1,  # 小王1张
    'D': 1   # 大王1张
}

# 玩家位置
POSITION_FIRST = 0   # 头叫
POSITION_SECOND = 1  # 二叫
POSITION_THIRD = 2   # 三叫

# 手牌数量
HAND_SIZE = 17
BASE_CARD_SIZE = 3

# ==========================================
# 训练配置
# ==========================================
CYCLE = 1000          # 总训练轮数
H = 200              # 每个agent每轮训练局数
BATCH = 600          # 可视化统计批次大小
SEGMENT_SIZE = 20    # 胜率统计分段大小
TRAIN_FREQUENCY = 40 # 训练频率（每N局游戏训练一次）

# ==========================================
# MCTS 配置
# ==========================================
USE_MCTS = True                    # 是否使用 MCTS
MCTS_NUM_SIMULATIONS = 50          # MCTS 模拟次数（50-400）
MCTS_C_PUCT = 1.0                  # 探索参数
MCTS_TEMPERATURE = 1.0             # 温度参数（1.0训练，0.0测试）

# ==========================================
# 并行训练配置
# ==========================================
USE_PARALLEL_MCTS = True           # 是否启用并行
PARALLEL_BATCH_SIZE = 8            # 并行游戏批次大小

# ==========================================
# 神经网络配置
# ==========================================
# 叫分阶段输入维度：17维（我的手牌）
BID_INPUT_DIM = 17

# 出牌阶段输入维度（75维）：
# - 我的手牌：17维
# - 剩余牌（外面还有哪些牌没出过）：17维
# - 上家预测：17维
# - 上家置信度：17维
# - 我的位置：1维
# - 上家位置：1维
# - 炸弹特征：5维（是否有炸弹、炸弹数量、最大炸弹点数、是否有王炸、王炸数量）
PLAY_INPUT_DIM = 75

HIDDEN_SIZE = 256
DROPOUT = 0.3

# 输出维度
BID_OUTPUT_DIM = 4  # 叫分：0/1/2/3

# ==========================================
# 强化学习配置
# ==========================================
LEARNING_RATE = 0.001
GAMMA = 0.99
EPSILON_START = 1.0
EPSILON_MIN = 0.05
EPSILON_DECAY = 0.995

# 奖励配置
REWARD_WIN = 10.0      # 获胜基础奖励
REWARD_LOSE = -10.0    # 失败基础奖励
# 注意：炸弹倍数在env.py中处理，每出一个炸弹最终分数×2

# ==========================================
# 自适应训练配置
# ==========================================
ADAPTIVE_TRAINING = True
WIN_RATE_THRESHOLD = 0.25
TRAINING_BOOST_FACTOR = 2.0
MAX_BOOST_FACTOR = 8.0
CONSECUTIVE_LOW_WIN_ROUNDS = 2

# ==========================================
# 路径配置
# ==========================================
MODEL_DIR = "models"
LOG_DIR = "logs"
PAIKU_FILE = "paiku.txt"

# 需要的牌组数量
NUM_PAIKU = 3 * CYCLE * H

# ==========================================
# 日志配置
# ==========================================
SAVE_GAME_LOGS = True
LOG_BUFFER_SIZE = H
